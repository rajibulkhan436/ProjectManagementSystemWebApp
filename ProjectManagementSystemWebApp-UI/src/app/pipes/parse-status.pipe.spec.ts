import { ParseStatusPipe } from './parse-status.pipe';

describe('ParseStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
